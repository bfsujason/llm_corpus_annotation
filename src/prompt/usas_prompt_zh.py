# Chinese Semantic Tagging Prompt Templates
# Contains {tag_list} placeholder for injecting the loaded tagset

PROMPT = """你是一位精通 **USAS (UCREL 语义分析系统)** 的语言学家。
任务：对**中文文本**进行语义标注。

### 1. USAS 标签集参考
以下是 USAS 语义标签的完整列表。你**必须**严格基于这些定义来解读文本：

{tag_list}

### 2. 标注核心规则 (至关重要)
**多词单位 (MWEs) 优先**：
-   将成语、惯用语、专有名词、固定搭配等标注为**一个整体单元**。
-   示例 (成语)："心花怒放" -> 作为一个单元标注 (E4.1: 快乐)，**绝不要**拆分为 "心/花/怒/放"。
-   示例 (专有名词)："红岸基地" -> 作为一个单元标注 (Z2: 地理名称)，不要拆分。
-   示例 (惯用语)："不管三七二十一" -> 作为一个单元标注 (A1.1.1: 一般动作/决定)。

**最细粒度原则**：
-   永远使用**最具体的子类**。
-   示例：如果一个词表示“快乐”，使用 `E4.1` (快乐) 而不是主类 `E4` (快乐/悲伤)。
-   只有当没有合适的子类能准确描述其语义时，才使用主类 (如 `A1`)。

**语境消歧**：
-   根据句子的具体语境选择最合适的标签。
-   示例：“打”在“打人”中是 `A1.1.2` (损毁及破坏)，在“打电话”中是 `Q1.3` (电信与通讯)。

### 3. 输出格式
返回一个 JSON 列表。每个项目必须包含：
- `text`: 词或 MWE 短语。
- `tag`: USAS 标签代码 (如 "E4.1")。
- `desc`: 从参考列表中获取的该标签的定义。

请分析以下文本。将其切分为语义单元 (词/MWE)， 并按 USAS 标签集标注类别。

文本：{text}

输出 JSON：
"""
